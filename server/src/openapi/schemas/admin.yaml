# Admin Mentee schemas - based on actual database and service
AdminMenteeItem:
  type: object
  properties:
    user_id:
      type: integer
      example: 1
    first_name:
      type: string
      example: "John"
    last_name:
      type: string
      example: "Doe"
    email:
      type: string
      format: email
      example: "john.doe@example.com"
    status:
      type: string
      enum: [Pending, Active, Inactive, Banned]
      example: "Active"
    goal:
      type: string
      nullable: true
      example: "Learn programming"

AdminMenteeListResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    message:
      type: string
      example: "Mentees retrieved successfully"
    data:
      type: array
      items:
        $ref: "#/AdminMenteeItem"
    pagination:
      $ref: "#/PaginationInfo"

AdminMenteeDetailResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    data:
      $ref: "#/AdminMenteeItem"

UpdateAdminMenteeRequest:
  type: object
  properties:
    first_name:
      type: string
      example: "John"
    last_name:
      type: string
      example: "Doe"
    email:
      type: string
      format: email
      example: "john.doe@example.com"
    goal:
      type: string
      example: "Learn web development"

# Admin Mentor schemas - based on actual database and service
AdminMentorItem:
  type: object
  properties:
    user_id:
      type: integer
      example: 5
    first_name:
      type: string
      example: "Jane"
    last_name:
      type: string
      example: "Smith"
    email:
      type: string
      format: email
      example: "jane.smith@example.com"
    status:
      type: string
      enum: [Pending, Active, Inactive, Banned]
      example: "Active"
    role:
      type: string
      example: "Mentor"
    bio:
      type: string
      nullable: true
      example: "Senior software engineer with 10 years experience"
    cv_url:
      type: string
      nullable: true
      example: "https://example.com/cv.pdf"
    headline:
      type: string
      nullable: true
      example: "Full Stack Developer"
    response_time:
      type: string
      example: "Within 24 hours"
    bank_name:
      type: string
      nullable: true
      example: "Vietcombank"
      description: "Name of the mentor's bank"
    account_number:
      type: string
      nullable: true
      example: "1234567890"
      description: "Bank account number (minimum 8 digits)"
    account_holder_name:
      type: string
      nullable: true
      example: "Jane Smith"
      description: "Name of the account holder"
    bank_branch:
      type: string
      nullable: true
      example: "District 1 Branch"
      description: "Bank branch location"
    swift_code:
      type: string
      nullable: true
      example: "BFTVVNVX"
      description: "SWIFT/BIC code (8 or 11 characters)"
    created_at:
      type: string
      format: date-time
      example: "2024-01-15T10:30:00.000Z"

AdminMentorPendingItem:
  type: object
  properties:
    user_id:
      type: integer
      example: 5
    first_name:
      type: string
      example: "Jane"
    last_name:
      type: string
      example: "Smith"
    email:
      type: string
      format: email
      example: "jane.smith@example.com"
    status:
      type: string
      enum: [Pending, Active, Inactive, Banned]
      example: "Active"
    role:
      type: string
      example: "Mentor"
    bio:
      type: string
      nullable: true
      example: "Senior software engineer with 10 years experience"
    cv_url:
      type: string
      nullable: true
      example: "https://example.com/cv.pdf"
    headline:
      type: string
      nullable: true
      example: "Full Stack Developer"
    response_time:
      type: string
      example: "Within 24 hours"
    created_at:
      type: string
      format: date-time
      example: "2024-01-15T10:30:00.000Z"

AdminMentorListResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    message:
      type: string
      example: "Mentors retrieved successfully"
    data:
      type: array
      items:
        $ref: "#/AdminMentorItem"
    pagination:
      $ref: "#/PaginationInfo"

AdminMentorPendingListResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    message:
      type: string
      example: "Mentors retrieved successfully"
    data:
      type: array
      items:
        $ref: "#/AdminMentorPendingItem"
    pagination:
      $ref: "#/PaginationInfo"

AdminMentorDetailResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    data:
      $ref: "#/AdminMentorItem"

UpdateAdminMentorRequest:
  type: object
  properties:
    first_name:
      type: string
      example: "Jane"
    last_name:
      type: string
      example: "Smith"
    email:
      type: string
      format: email
      example: "jane.smith@example.com"
    bio:
      type: string
      example: "Updated bio"
    headline:
      type: string
      example: "Senior Developer"
    response_time:
      type: string
      example: "Within 12 hours"
    cv_url:
      type: string
      example: "https://example.com/new-cv.pdf"
    bank_name:
      type: string
      example: "Vietcombank"
      description: "Name of the mentor's bank (minimum 2 characters)"
    account_number:
      type: string
      example: "1234567890"
      description: "Bank account number (minimum 8 digits, numbers only)"
    account_holder_name:
      type: string
      example: "Jane Smith"
      description: "Name of the account holder (minimum 2 characters)"
    bank_branch:
      type: string
      example: "District 1 Branch"
      description: "Bank branch location (minimum 2 characters)"
    swift_code:
      type: string
      example: "BFTVVNVX"
      description: "SWIFT/BIC code (8 or 11 characters, uppercase letters)"

# Review Mentor schemas
ReviewMentorRequest:
  type: object
  required:
    - action
  properties:
    action:
      type: string
      enum: [accept, reject]
      description: "accept: set status to Inactive and send approval email. reject: delete mentor and send rejection email."
      example: "accept"

ReviewMentorResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    message:
      type: string
      example: "Mentor accepted"

# Pagination schema
PaginationInfo:
  type: object
  properties:
    currentPage:
      type: integer
      example: 1
    totalPages:
      type: integer
      example: 5
    totalItems:
      type: integer
      example: 50
    itemsPerPage:
      type: integer
      example: 10
    hasNextPage:
      type: boolean
      example: true
    hasPreviousPage:
      type: boolean
      example: false

# Invoice Stats schemas
InvoiceItem:
  type: object
  properties:
    invoice_id:
      type: integer
      example: 1
    method:
      type: string
      example: "Stripe"
    paid_time:
      type: string
      format: date-time
      example: "2024-12-05T10:30:00.000Z"
    payment_status:
      type: string
      example: "Completed"
    currency:
      type: string
      example: "USD"
    amount_subtotal:
      type: number
      format: decimal
      example: 99.99
    amount_total:
      type: number
      format: decimal
      example: 99.99
    stripe_receipt_url:
      type: string
      nullable: true
      example: "https://stripe.com/receipt/123"
    plan_description:
      type: string
      example: "1-on-1 mentorship session"
    plan_type:
      type: string
      example: "Session"
    plan_charge:
      type: number
      format: decimal
      example: 99.99
    mentee_id:
      type: integer
      example: 31
    mentee_first_name:
      type: string
      example: "Alice"
    mentee_last_name:
      type: string
      example: "Smith"
    mentee_email:
      type: string
      example: "alice.smith@example.com"
    mentee_avatar_url:
      type: string
      nullable: true
      example: "https://example.com/avatar.jpg"
    mentor_id:
      type: integer
      example: 1
    mentor_first_name:
      type: string
      example: "John"
    mentor_last_name:
      type: string
      example: "Doe"
    mentor_email:
      type: string
      example: "john.doe@example.com"
    mentor_avatar_url:
      type: string
      nullable: true
      example: "https://example.com/mentor-avatar.jpg"

InvoiceStatsResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    message:
      type: string
      example: "Invoice statistics retrieved successfully"
    data:
      type: object
      properties:
        year:
          type: integer
          example: 2024
        month:
          type: integer
          example: 12
        total_amount:
          type: number
          format: decimal
          example: 1499.85
        total_count:
          type: integer
          example: 15
        invoices:
          type: array
          items:
            $ref: "#/InvoiceItem"
        pagination:
          $ref: "#/PaginationInfo"

# System Statistics Response
SystemStatsResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    message:
      type: string
      example: "System statistics retrieved successfully"
    data:
      type: object
      properties:
        users:
          type: object
          properties:
            total:
              type: integer
              example: 100
            mentors:
              type: integer
              example: 30
            mentees:
              type: integer
              example: 68
            admins:
              type: integer
              example: 2
        mentors:
          type: object
          properties:
            active:
              type: integer
              example: 25
            pending:
              type: integer
              example: 3
            inactive:
              type: integer
              example: 1
            banned:
              type: integer
              example: 1
        mentees:
          type: object
          properties:
            active:
              type: integer
              example: 65
            inactive:
              type: integer
              example: 2
            banned:
              type: integer
              example: 1
        bookings:
          type: object
          properties:
            total:
              type: integer
              example: 150
            thisMonth:
              type: integer
              example: 25
            lastMonth:
              type: integer
              example: 20
        invoices:
          type: object
          properties:
            total:
              type: integer
              example: 145
            totalRevenue:
              type: number
              format: decimal
              example: 14550.75
            thisMonthRevenue:
              type: number
              format: decimal
              example: 2500.00
            lastMonthRevenue:
              type: number
              format: decimal
              example: 2000.00
            averageInvoiceAmount:
              type: number
              format: decimal
              example: 100.35
        plans:
          type: object
          properties:
            totalPlans:
              type: integer
              example: 45
            sessionPlans:
              type: integer
              example: 30
            mentorshipPlans:
              type: integer
              example: 15
        meetings:
          type: object
          properties:
            total:
              type: integer
              example: 200
            completed:
              type: integer
              example: 150
            upcoming:
              type: integer
              example: 40
            cancelled:
              type: integer
              example: 10

# Dashboard Statistics Schemas
MentorDashboardStats:
  type: object
  properties:
    mentor_id:
      type: integer
      example: 5
    mentor_name:
      type: string
      example: "John Doe"
    mentor_email:
      type: string
      example: "john.doe@example.com"
    mentor_avatar_url:
      type: string
      nullable: true
      example: "https://example.com/avatar.jpg"
    mentor_country:
      type: string
      nullable: true
      example: "United States"
    mentor_status:
      type: string
      example: "Active"
    mentor_headline:
      type: string
      nullable: true
      example: "Senior Software Engineer"
    company_names:
      type: string
      example: "Google, Microsoft"
    job_titles:
      type: string
      example: "Software Engineer, Tech Lead"
    skills:
      type: string
      example: "Python, JavaScript, React"
    total_bookings:
      type: integer
      example: 25
    total_invoices:
      type: integer
      example: 25
    total_revenue:
      type: number
      format: decimal
      example: 2500.00
    average_invoice_amount:
      type: number
      format: decimal
      example: 100.00
    completed_meetings:
      type: integer
      example: 20
    cancelled_meetings:
      type: integer
      example: 2
    upcoming_meetings:
      type: integer
      example: 3
    average_rating:
      type: number
      format: decimal
      example: 4.5
    total_reviews:
      type: integer
      example: 15
    total_plans:
      type: integer
      example: 5

MonthlyDashboardStats:
  type: object
  properties:
    year:
      type: integer
      example: 2024
    month:
      type: integer
      example: 6
    month_name:
      type: string
      example: "June"
    total_invoices:
      type: integer
      example: 45
    total_mentees:
      type: integer
      example: 30
    total_mentors:
      type: integer
      example: 15
    total_revenue:
      type: number
      format: decimal
      example: 4500.00
    average_invoice_amount:
      type: number
      format: decimal
      example: 100.00
    total_bookings:
      type: integer
      example: 45

CategoryDashboardStats:
  type: object
  properties:
    category_id:
      type: integer
      example: 3
    category_name:
      type: string
      example: "Web Development"
    total_mentors_with_skill:
      type: integer
      example: 12
    total_bookings:
      type: integer
      example: 85
    total_revenue:
      type: number
      format: decimal
      example: 8500.00

DashboardStatsData:
  type: object
  properties:
    groupBy:
      type: string
      enum: [mentor, month, category]
      example: "mentor"
    mentorStats:
      type: array
      nullable: true
      items:
        $ref: "#/MentorDashboardStats"
    monthlyStats:
      type: array
      nullable: true
      items:
        $ref: "#/MonthlyDashboardStats"
    categoryStats:
      type: array
      nullable: true
      items:
        $ref: "#/CategoryDashboardStats"

DashboardStatsResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    message:
      type: string
      example: "Dashboard statistics retrieved successfully"
    data:
      $ref: "#/DashboardStatsData"

# Common response schemas
AdminSuccessResponse:
  type: object
  properties:
    success:
      type: boolean
      example: true
    message:
      type: string
      example: "Operation successful"
